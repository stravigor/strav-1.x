// Forward-compat placeholder — the M1 kernel doesn't ship a Logger yet, but the
// config slot is already wired so subsequent milestones can read it as-is.
export default {
  level: 'info',
  channel: 'stderr',
}
